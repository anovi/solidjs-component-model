// Generated by dts-bundle-generator v9.5.1

import { Accessor } from 'solid-js';
import { SetStoreFunction, Store } from 'solid-js/store';

export interface Unsubscribable {
	unsubscribe: () => void;
}
export interface Subscribable<T> {
	subscribe: (observer: Partial<Observer<T>>) => Unsubscribable;
}
export interface SubscriptionLike extends Unsubscribable {
	readonly closed: boolean;
	unsubscribe: () => void;
}
export interface Observer<T> {
	next: (value: T) => void;
	error: (err: unknown) => void;
	complete: () => void;
}
export interface Observable<T> extends Subscribable<T> {
	subscribe: (observerOrNext?: Partial<Observer<T>> | ((value: T) => void)) => SubscriptionLike;
}
/** Context in which actions and guards execute. */
export type ExecutionContext = any;
type Event$1 = {
	type: string;
};
export type EventName<E> = E extends {
	type: infer T extends PropertyKey;
} ? T : never;
export type TransitionStep<TModel, E extends Event$1> = {
	path: string;
	exit: boolean;
	final?: boolean;
	effect?: Action<TModel, E>;
};
export type AnyStateChartConfig = StateChartConfig<any, any>;
export type StateChartConfig<TModel, E extends Event$1> = {
	/** Which child state to enter initially. Is required when the state has child states. */
	initial?: string;
	/** Child states */
	states?: {
		[state: string]: StateChartConfig<TModel, E>;
	};
	type?: "parallel" | "final" | "history";
	/** An entry effect executed when machine enters this state. */
	entry?: Action<TModel, E>;
	/** An exit effect executed just before machine exits this state. */
	exit?: Action<TModel, E>;
	/** Always transition that is checked after handling any other event. */
	always?: HandlerForEvent<TModel, never, never> | HandlerForEvent<TModel, never, never>[];
	/** An object whose keys are event names and values are transitions. */
	on?: {
		[K in EventName<E>]?: HandlerForEvent<TModel, E, K> | HandlerForEvent<TModel, E, K>[];
	};
};
export type Transition<TModel, E extends Event$1> = {
	target?: string;
	reenter?: boolean;
	guard?: Guard<TModel, E>;
	action?: Action<TModel, E>;
};
export type HandlerForEvent<TModel, E extends Event$1, T extends EventName<E>> = Transition<TModel, Extract<E, {
	type: T;
}>>;
export type Guard<TModel, E extends Event$1> = (this: TModel, ev: E) => boolean;
export type AnyAction = Action<unknown, Event$1>;
export type Action<TModel, E extends Event$1> = (this: TModel, ev: E) => void;
declare enum NodeType {
	parallel = 1,
	final = 2,
	history = 3
}
export type AnyStateNode = StateNode<any, any>;
export type StateNode<TModel, E extends Event$1> = {
	name: string;
	parent: StateNode<TModel, E> | null;
	initial?: StateNode<TModel, E>;
	children?: {
		[state: string]: StateNode<TModel, E>;
	};
	type?: NodeType;
	entry?: Action<TModel, E>;
	exit?: Action<TModel, E>;
	always?: HandlerForEvent<TModel, never, never>[];
	on?: {
		[K in EventName<E>]?: HandlerForEvent<TModel, E, K>[];
	};
};
/**
 * State chart does not own data or effects.
 *
 * It only tells what transitions should be when you pass event to it.
*/
export declare class StateChart<TModel, E extends Event$1 = Event$1, TConfig = StateChartConfig<TModel, E>> {
	#private;
	root: AnyStateNode;
	lookup: Map<string, AnyStateNode>;
	config: TConfig;
	static create<TContext = any, E extends Event$1 = Event$1, TConfig extends StateChartConfig<TContext, E> = StateChartConfig<TContext, E>>(config: StateChartConfig<TContext, E> & TConfig): StateChart<TContext, E, TConfig>;
	getNodeByPath(path: string): AnyStateNode | undefined;
	getEventHandlers(path: string, event?: Event$1): Transition<TModel, any>[] | undefined;
	resolveFinalState(path?: string): string;
	createRuntime(context: TModel): Interpreter<TModel, E>;
}
/**
 * Runtime interpreter of a state chart.
 * It's stateless, it only has two methods which is pure functions.
*/
export declare class Interpreter<TModel extends ExecutionContext, E extends Event$1 = Event$1> {
	private context;
	private readonly chart;
	constructor(context: TModel, chart: StateChart<TModel, E, any>);
	/**
	 * Returns a generator with micro-steps you need to perform to make a transition.
	 *
	 * For starting transition call `.transition('', '')`
	*/
	transition(fromStateString: string, toStateString: string, reenter?: boolean): Generator<TransitionStep<TModel, E>>;
	/**
	 * Get a handler for an event in a given state.
	 *
	 * Lookup a handler starting from deepest state, moving up, executing guards.
	 * Returns the first matched transition.
	 *
	 * If no event is passed, it checks for an eventless transition (always), deep first as well.
	 *
	 * @see {@link http://localhost:5173/event-loop#transition-lookup|Transition lookup}
	 */
	getMostSpecificHandler(pathString: string, event?: E): Transition<TModel, any> | Error | undefined;
}
export declare class MachineMalformed extends Error {
	readonly name = "Machine Malformed";
	constructor(message: string, options?: {
		cause?: unknown;
		machineConfig: unknown;
	});
}
export interface Model<Data extends AnyModelData = AnyModelData, E extends Event$1 = {
	type: string;
}, Emitted extends Event$1 = {
	type: string;
}, DoneData = unknown> {
	readonly _id: string;
	data: Store<Data>;
	status: Status;
	/** Error that caused stopping the machine with `error` status */
	error?: Error;
	doneData?: DoneData;
	on: <T extends EventType<Emitted>>(type: T, handler: (event: Extract<Emitted, {
		type: T;
	}>) => void) => Unsubscribable;
	subscribe: (observerOrNext: Partial<Observer<Emitted>>) => Unsubscribable;
	dispatch: (event: E) => void;
	toJSON: () => Snapshot<string, AnyModelData>;
	getPersistedSnapshot: () => unknown;
	start: () => void;
	stop: () => void;
}
export interface Logger {
	event: (event: Event$1 | InternalEvent) => void;
	transition: (from: string, to: string) => void;
	warning: (message: string) => void;
	group: (name: string, id: string) => void;
	groupEnd: () => void;
	error: (message: string, options?: {
		cause?: unknown;
	}) => void;
	effectError: (type: "event" | "entry" | "exit", state: string, error: Error) => void;
}
export declare class BrowserLogger implements Logger {
	event(event: Event$1 | InternalEvent): void;
	transition(from: string, to: string): void;
	warning(message: string): void;
	effectError(type: string, state: string, error: Error): void;
	group(name: string, id: string): void;
	groupEnd(): void;
	error(message: string, options?: {
		cause?: unknown;
	}): void;
}
export declare class TerminalLogger implements Logger {
	event(event: Event$1 | InternalEvent): void;
	transition(from: string, to: string): void;
	warning(message: string): void;
	effectError(type: "event" | "entry" | "exit", state: string, error: Error): void;
	group(name: string, id: string): void;
	groupEnd(): void;
	error(message: string, options?: {
		cause?: unknown;
	}): void;
}
/**
 * Observability types and interfaces (contract only).
 * Domain, Application, and UI depend on these; infra implements them.
 */
export type TraceId = string;
export type SpanId = string;
export interface TraceContext {
	traceId: TraceId;
	spanId: SpanId;
}
export type SpanStatus = "ok" | "error" | "cancelled";
export interface SpanAttributes {
	[key: string]: string | number | boolean | undefined;
}
export interface Span {
	readonly traceId: TraceId;
	readonly spanId: SpanId;
	readonly parentSpanId?: SpanId;
	readonly name: string;
	setAttribute: (key: string, value: string | number | boolean) => void;
	setAttributes: (attributes: SpanAttributes) => void;
	/**
	 * Record an error on the span.
	 * Should also set status to "error".
	 */
	recordError: (error: unknown) => void;
	/**
	 * Mark span as completed.
	 * Must: capture endTime, compute duration, trigger export.
	 * Should be idempotent.
	 */
	end: (status?: SpanStatus) => void;
	/**
	 * Return minimal context used for propagation.
	 */
	context: () => TraceContext;
}
export interface Tracer {
	startTrace: (name: string, attributes?: SpanAttributes) => Span;
	startSpan: (name: string, options?: {
		parent?: TraceContext;
		attributes?: SpanAttributes;
	}) => Span;
}
export type AnyModelData = Record<string, unknown>;
export type AnyModel = Model<any, any, any, any>;
export type Cleanup = void | (() => void);
export type Invoke<E> = (ctx: {
	signal: AbortSignal;
	send: (event: E) => void;
}) => Cleanup | Promise<Cleanup>;
export type Status = "idle" | "active" | "stopped" | "error" | "done";
export type Snapshot<State extends string, Data extends AnyModelData> = {
	_id: string;
	name: string;
	state: State;
	data: Data;
	status: Status;
};
export interface FrameworkConfig {
	logger?: Logger;
	tracer?: Tracer;
}
export type EventType<E> = E extends {
	type: infer T;
} ? T : never;
export declare const InternalEventName: {
	readonly InvokedDone: "@done.invoke";
	readonly InvokedError: "@error.invoke";
	readonly InvokedNext: "@next.invoke";
	readonly ScheduledExecute: "@execute.scheduled";
	readonly Eventless: "@always";
	readonly Start: "@start";
};
export type Start = {
	type: typeof InternalEventName.Start;
};
export type Eventless = {
	type: typeof InternalEventName.Eventless;
	state: string;
};
export type InvokedDone<T = unknown> = {
	type: typeof InternalEventName.InvokedDone;
	result: T;
	state: string;
};
export type InvokedError = {
	type: typeof InternalEventName.InvokedError;
	error: unknown;
	state: string;
};
export type InvokedNext<V = unknown> = {
	type: typeof InternalEventName.InvokedNext;
	value: V;
	state: string;
};
export type ScheduledExecute = {
	type: typeof InternalEventName.ScheduledExecute;
	state: string;
};
export type InternalEvent = Start | Eventless | InvokedNext | InvokedDone | InvokedError | ScheduledExecute;
export type L1<T, P extends string = ""> = T extends {
	states?: infer S extends Record<string, any>;
} ? {
	[K in keyof S & string]: `${P}${K}`;
}[keyof S & string] : never;
export type L2<T, P extends string = ""> = T extends {
	states?: infer S extends Record<string, any>;
} ? {
	[K in keyof S & string]: `${P}${K}` | L1<S[K], `${P}${K}.`>;
}[keyof S & string] : never;
export type L3<T, P extends string = ""> = T extends {
	states?: infer S extends Record<string, any>;
} ? {
	[K in keyof S & string]: `${P}${K}` | L2<S[K], `${P}${K}.`>;
}[keyof S & string] : never;
export type StatePaths<T> = L3<T>;
export type SendApi<E extends {
	type: string;
}> = {
	[K in EventName<E>]: (payload?: Omit<Extract<E, {
		type: K;
	}>, "type">) => void;
};
export interface ModelConstructor<TModel extends AnyModel, E extends Event$1> {
	new (...args: any[]): AnyComponentModel;
	chart?: StateChart<TModel, E>;
}
export type AnyModelConstructor = ModelConstructor<AnyModel, Event$1>;
export type AnyComponentModel = ComponentModel<any, any, any, any>;
export type ChildTypes = {
	[key: string]: AnyModelConstructor;
};
/**
 * Decorator that wraps a method so its body is enqueued as an effect
 * and the queue is processed. Use on methods in classes extending ComponentModel.
 */
export declare function action(_target: object, _propertyKey: string, descriptor: PropertyDescriptor): PropertyDescriptor;
export declare abstract class ComponentModel<Data extends AnyModelData = AnyModelData, E extends Event$1 = {
	type: string;
}, Emitted extends Event$1 = {
	type: string;
}, DoneData = unknown> implements Model<Data, E, Emitted, DoneData> {
	#private;
	static childTypes: ChildTypes;
	data: Store<Data>;
	state: Accessor<StatePaths<StateChartConfig<this, E>>>;
	/** Error that caused stopping the machine with `error` status */
	error?: Error;
	readonly send: SendApi<E>;
	status: Status;
	doneData?: DoneData;
	_id: `${string}-${string}-${string}-${string}-${string}`;
	constructor(ctx: Data);
	static configure(config: Partial<FrameworkConfig>): void;
	static fromJSON<TThis extends new (...args: any[]) => AnyComponentModel>(this: TThis, snapshot: unknown): InstanceType<TThis>;
	static fromPersistedSnapshot<TThis extends new (...args: any[]) => AnyComponentModel>(this: TThis, snapshot: unknown): InstanceType<TThis>;
	/** Override to apply extra fields from a persisted snapshot to `this`. */
	protected applyPersistedSnapshot(_snapshot: unknown): void;
	on<T extends EventType<Emitted>>(type: T, handler: (event: Extract<Emitted, {
		type: T;
	}>) => void): Unsubscribable;
	subscribe(observer: Partial<Observer<Emitted>>): Unsubscribable;
	dispatch(event: E): void;
	toJSON(): Snapshot<string, Data>;
	getPersistedSnapshot(): unknown;
	start(): void;
	stop(): void;
	protected parent?: any;
	protected invokeObservable<Next>(observable: Observable<Next>, handler: {
		next?: Transition<ComponentModel<Data, E, Emitted>, InvokedNext<Next>>;
		error?: Transition<ComponentModel<Data, E, Emitted>, InvokedError>;
		complete?: Transition<ComponentModel<Data, E, Emitted>, InvokedDone<undefined>>;
	}): void;
	protected invokePromise<T>(promise: (signal: AbortSignal) => Promise<T>, params: {
		onDone: Transition<ComponentModel<Data, any, Emitted>, InvokedDone<T>> | Transition<ComponentModel<Data, E, Emitted>, InvokedDone<T>>[];
		onError?: Transition<ComponentModel<Data, any, Emitted>, InvokedError>;
	}): void;
	protected emit(event: Emitted): void;
	protected onCleanup?: () => void;
	protected setData: SetStoreFunction<Data>;
	protected schedule(handler: Transition<this, E | InternalEvent>, timeout?: number): void;
	protected enqueue(task: () => void, parentSpan?: Span): void;
	protected get logger(): Logger | undefined;
	protected set logger(logger: Logger | null);
	protected get tracer(): Tracer | undefined;
	protected set tracer(tracer: Tracer | null);
	private readonly stateChart?;
	private static defaults;
	/** Redefine method if you have highly customized model. */
	private static fromSnapshot;
	private static dataFromJSON;
	private static isModelSnapshot;
	private static getChildCtor;
}
export type Constructor<T = object> = new (...args: any[]) => T;
export interface StateChartMethods<TConfig> {
	matches: (path: StatePaths<TConfig>) => boolean;
}
export type WithStateChartConstructor<TBase extends Constructor, TConfig> = Omit<TBase, "prototype"> & {
	new (...args: ConstructorParameters<TBase>): InstanceType<TBase> & StateChartMethods<TConfig>;
	config: TConfig;
};
export declare function WithStateChart<TBase extends Constructor<ComponentModel<any, any, any, any>>, E extends Event$1 = InstanceType<TBase> extends ComponentModel<any, infer EE, any, any> ? EE : Event$1, TConfig extends StateChartConfig<InstanceType<TBase>, E> = StateChartConfig<InstanceType<TBase>, E>>(Base: TBase, config: TConfig, name?: string): WithStateChartConstructor<TBase, TConfig>;
export declare function WithStateChart<TBase extends Constructor<ComponentModel<any, any, any, any>>, E extends Event$1 = InstanceType<TBase> extends ComponentModel<any, infer EE, any, any> ? EE : Event$1, TConfig extends StateChartConfig<InstanceType<TBase>, E> = StateChartConfig<InstanceType<TBase>, E>>(Base: TBase, chart: StateChart<InstanceType<TBase>, E, TConfig>, name?: string): WithStateChartConstructor<TBase, TConfig>;

export {
	Event$1 as Event,
};

export {};
